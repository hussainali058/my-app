[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  port = 3000

[[redirects]]
  from = "/api/*"
  to = "https://web-production-1174.up.railway.app/api/:splat"
  status = 200
  force = true
